# Example secrets file
# Copy this to secrets.yaml and encrypt with sops:
#   sops -e secrets.yaml > encrypted_secrets.yaml
#
# To use:
#   1. Generate age key: age-keygen -o ~/.config/sops/age/keys.txt
#   2. Create .sops.yaml in dotfiles root with your public key
#   3. Encrypt this file with sops

openweather_api_key: YOUR_OPENWEATHER_API_KEY_HERE

# Add other secrets as needed:
# github_token: ghp_xxxxxxxxxxxx
# some_other_secret: value
